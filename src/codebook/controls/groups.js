/*------------------------------------------------------------------------------------------------\
  Define filter controls object.
\------------------------------------------------------------------------------------------------*/

import { init } from './groups/init';
import { update } from './groups/update';

export const groups = {
    init: init,
    update: update
};
